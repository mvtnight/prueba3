spring:
  datasource:

    url: jdbc:mysql://localhost:3306/rednorte_usuarios?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: user_usuarios
    password: devpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true